{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/projects/(.*)", "dest": "/projects/$1.html" }
    // { "src": "/(.*)", "dest": "/index.html" }
  ]
}
